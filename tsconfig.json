{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2016",                  /* Set the JavaScript language version for emitted JavaScript. */
    "module": "CommonJS",                /* Specify the module code format (CommonJS for Node.js). */
    "rootDir": "./src",                  /* Root folder of the input files. */
    "outDir": "./dist",                  /* Directory where output files are placed. */
    "declaration": true,                 /* Generate .d.ts files for TypeScript declarations. */
    "declarationDir": "./dist",          /* Output directory for declaration files. */
    "esModuleInterop": true,             /* Ease support for importing CommonJS modules. */
    "forceConsistentCasingInFileNames": true, /* Enforce consistent casing in module imports. */
    "strict": true,                      /* Enable all strict type-checking options. */
    "skipLibCheck": true,                /* Skip type checking for declaration files. */

    /* Type Checking */
    "noImplicitAny": true,               /* Enable error reporting for expressions and declarations with an implied 'any' type. */
    "strictNullChecks": true,            /* Enable strict null checking for types. */
    "strictFunctionTypes": true,         /* Ensure parameters and return values are subtype-compatible. */
    "noImplicitThis": true,              /* Enable error reporting when 'this' is given the type 'any'. */
    "useUnknownInCatchVariables": true,  /* Default catch clause variables as 'unknown'. */
    
    /* Emit */
    "sourceMap": true,                   /* Generate .map files for debugging. */

    /* Module Resolution */
    "moduleResolution": "node",          /* Use Node.js module resolution strategy. */
    "resolveJsonModule": true,           /* Allow importing JSON modules. */

    /* Advanced Options */
    "allowSyntheticDefaultImports": true /* Allow default imports from modules without default exports. */
  },
  "include": ["src/**/*"],                /* Include all TypeScript files in the `src` folder. */
  "exclude": ["node_modules", "dist"]     /* Exclude unnecessary files and folders. */
}
